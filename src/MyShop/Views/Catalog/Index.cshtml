@model MyShop.Models.CatalogIndexViewModel

@{
    ViewData["Title"] = "Catalog";
}

<section>
    <div>
        <form method="get">
            <label data-title ="brand">
                <select asp-for="@Model.BrandFilterApplied" asp-items="@Model.Brands"></select>
            </label>
            <label data-title="type">
                <select asp-for="@Model.TypesFilterApplied" asp-items="@Model.Types"></select>
            </label>
            <input type="submit" value="Apply Filters"/>
        </form>
    </div>
</section>
<div class="container">
    @if (Model.CatalogItems.Any())
    {
        <div class="row">
            @foreach(var catalogItem in Model.CatalogItems)
            {
                <img src="@catalogItem.PictureUrl"/>
                <div>
                    <span>@catalogItem.Name</span>
                </div>
                <div>
                    <span>@catalogItem.Price</span>
                </div>
            }
        </div>
    }
</div>


@*<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PictureUrl)              
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Price)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                    <img style="border-radius:10px; border: 1px solid" width="150px"
                     src="@Html.DisplayFor(model => item.PictureUrl)" />

            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>*@
